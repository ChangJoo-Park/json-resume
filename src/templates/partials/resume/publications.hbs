{{#> section}}
  {{#each data as |publication|}}
    {{publication.name}}
    {{publication.publisher}}
    {{publication.releaseDate}}
    {{publication.website}}
    {{publication.summary}}
  {{/each}}
{{/section}}
